const desktopSize = 1600
const tabletSize = 1200
const mobileSize = 800

export const DESKTOP_SIZE = `(max-width: ${desktopSize}px)`;
export const TABLET_SIZE = `(max-width: ${tabletSize}px)`;
export const MOBILE_SIZE = `(max-width: ${mobileSize}px)`;
